[project]
name = "pose-estimation"
version = "0.1.0"
description = "Real-time 3D pose estimation with MediaPipe"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mediapipe>=0.10.14",
    "opencv-python>=4.10.0",
    "numpy>=1.26.0",
]
